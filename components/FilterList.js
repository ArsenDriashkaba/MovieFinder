const FilterList = ({}) => {
  return (
    <ul className={styles.container}>
      <li>
        <button>Price</button>
      </li>
      <li>
        <button>Popular</button>
      </li>
    </ul>
  );
};

export default FilterList;
